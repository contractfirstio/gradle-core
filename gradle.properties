# Enable Gradle configuration cache by default
org.gradle.configuration-cache=true
# Fail the build on any configuration cache problem
org.gradle.configuration-cache.problems=fail
# Fail if any configuration cache problem is detected
org.gradle.configuration-cache.max-problems=0

